name: Deploy to Lightsail BFF

on:
  push:
    branches:
      - main
    paths:
      - 'lightsail-bff/**'
      - '.github/workflows/deploy-lightsail-bff.yml'

jobs:
  deploy:
    name: Deploy BFF to Lightsail
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Prepare SSH key
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "${{ secrets.LIGHTSAIL_SSH_KEY }}" ]; then
            echo "::error::Missing GitHub secret LIGHTSAIL_SSH_KEY. Set it to your *private* SSH key (OpenSSH format), or a base64-encoded private key."
            exit 1
          fi

          # NOTE: appleboy/scp-action runs in a container. If we put the key under
          # a restrictive directory (e.g. .ssh with 700 perms), the container user
          # can get "permission denied" reading it. Keep the key in the workspace
          # and make it readable for this ephemeral runner only.
          KEY_FILE="${GITHUB_WORKSPACE}/lightsail.key"

          # Support either a raw multiline private key, or a base64-encoded key.
          if printf '%s' "${{ secrets.LIGHTSAIL_SSH_KEY }}" | grep -q "BEGIN .*PRIVATE KEY"; then
            printf '%s' "${{ secrets.LIGHTSAIL_SSH_KEY }}" > "${KEY_FILE}"
          else
            printf '%s' "${{ secrets.LIGHTSAIL_SSH_KEY }}" | base64 -d > "${KEY_FILE}"
          fi

          # Readable by the scp/ssh action containers.
          chmod a+r "${KEY_FILE}"

      - name: Upload BFF files to Lightsail
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 13.236.58.12
          username: ubuntu
          key_path: lightsail.key
          port: 22
          passphrase: ${{ secrets.LIGHTSAIL_SSH_KEY_PASSPHRASE }}
          source: "lightsail-bff/*"
          target: "/home/ubuntu/lightsail-bff"

      - name: Deploy to Lightsail via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 13.236.58.12
          username: ubuntu
          key_path: lightsail.key
          port: 22
          passphrase: ${{ secrets.LIGHTSAIL_SSH_KEY_PASSPHRASE }}
          script_stop: true
          script: |
            echo "📦 Deploying BFF from uploaded artefacts..."

            BASE_DIR="/home/ubuntu/lightsail-bff"
            if [ -d "${BASE_DIR}/lightsail-bff" ]; then
              SRC_DIR="${BASE_DIR}/lightsail-bff"
            elif [ -f "${BASE_DIR}/package.json" ]; then
              # Some scp configurations unpack files directly into the target directory.
              SRC_DIR="${BASE_DIR}"
            else
              echo "❌ Uploaded folder not found at expected locations."
              echo "Contents of ${BASE_DIR}:"
              ls -la "${BASE_DIR}" || true
              exit 1
            fi

            # Copy BFF files from uploaded folder to app directory
            echo "📋 Syncing BFF files..."
            sudo -u deployer rsync -av --delete --exclude='.env' --exclude='node_modules' "${SRC_DIR}/" /home/deployer/app/ || exit 1
            
            # Install dependencies
            echo "📦 Installing dependencies..."
            cd /home/deployer/app
            sudo -u deployer npm ci --production || exit 1
            
            # Restart service
            echo "🔄 Restarting BFF service..."
            sudo systemctl restart clinicpro-bff || exit 1
            
            # Verify service is running
            echo "✅ Checking service status..."
            sleep 3
            sudo systemctl is-active clinicpro-bff || exit 1
            
            echo "🎉 Deployment successful!"
            echo "Service status:"
            sudo systemctl status clinicpro-bff --no-pager | head -10

      - name: Comment on commit
        uses: peter-evans/commit-comment@v3
        if: success()
        with:
          body: |
            ✅ **Lightsail BFF deployed successfully!**
            
            - Server: 13.236.58.12
            - Service: clinicpro-bff
            - Status: Running
            
            Check deployment logs in [Actions](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
