# FHIR MCP Server Configuration for Development
# This file configures the FHIR MCP Server for FHIR API exploration and development

# ====================================================================
# IMPORTANT: Medtech ALEX API Limitation
# ====================================================================
# The FHIR MCP Server does NOT work directly with Medtech ALEX API because
# ALEX requires custom headers (mt-facilityid, mt-correlationid, mt-appid)
# that the MCP server doesn't inject.
#
# Use this MCP server for:
# 1. Learning FHIR operations and resource structures
# 2. Testing FHIR queries on public servers (HAPI)
# 3. Prototyping queries before implementing in your BFF
#
# For actual ALEX API testing, continue using your BFF at api.clinicpro.co.nz
# ====================================================================

# Default: Public HAPI FHIR Test Server (works without auth)
FHIR_SERVER_BASE_URL=https://hapi.fhir.org/baseR4

# Disable authorization for public HAPI server
FHIR_SERVER_DISABLE_AUTHORIZATION=True

# MCP Server Configuration
FHIR_MCP_HOST=localhost
FHIR_MCP_PORT=8000

# Request timeout (seconds)
FHIR_MCP_REQUEST_TIMEOUT=30

# ====================================================================
# Medtech ALEX API Configuration (for reference - won't work directly)
# ====================================================================
# To connect to ALEX, you'd need these settings, BUT the MCP server
# can't add the required custom headers, so this won't work:
#
# FHIR_SERVER_BASE_URL=https://alexapiuat.medtechglobal.com/FHIR
# FHIR_SERVER_CLIENT_ID=7685ade3-f1ae-4e86-a398-fe7809c0fed1
# FHIR_SERVER_CLIENT_SECRET=Zub8Q~oBMwpgCJzif6Nn2RpRlIbt6q6g1y3ZhcID
# FHIR_SERVER_SCOPES=api://bf7945a6-e812-4121-898a-76fea7c13f4d/.default
# FHIR_SERVER_DISABLE_AUTHORIZATION=False
#
# Required headers (not supported by MCP server):
# - mt-facilityid: F99669-C
# - mt-correlationid: <UUID>
# - mt-appid: ClinicPro
# ====================================================================
